f(n)=f(n-1)+f(n-2) 后端接口直接上线 会有什么风险？

原始问题
分析以下SQL语句在不同索引配置下的执行情况：

sql
1. select * from t where id = x and name = x
2. select * from t where name = x and id = x
3. select * from t where name = x or id = x
4. select * from t where id = x or name = x
索引配置场景分析
场景一：单列索引配置
id 列有单列索引

name 列有单列索引

场景二：联合索引 (name, id) 配置
存在 (name, id) 联合索引

场景三：联合索引 (id, name) 配置
存在 (id, name) 联合索引

调用两个接口（扣积分、发货） 如何确保两个接口一起成功或者一起失败？